<h1>マイページ</h1>
登録情報
<%= link_to "編集する", customers_information_edit_path, class: "btn btn-success" %>

氏名<%= @customer.first_name %><%= @customer.last_name %>
カナ<%= @customer.first_name_kana %><%= @customer.last_name_kana %>
郵便番号<%= @customer.postal_code %>
住所<%= @customer.address %>
電話番号<%= @customer.telephone_number %>
メールアドレス<%= @customer.email %>



<p>配送先</p>
<%= link_to "一覧を見る"%>
<p>注文履歴</p>
<%= link_to "一覧を見る"%>

<table class="table table-bordered">
<thead>
  <tbody>
    <tr class="table-active">
      ...
    </tr>
    <tr>
      ...
    </tr>
    <tr>
      <th scope="row">3</th>
      <td colspan="6" class="table-active">Larry the Bird</td>
      <td>@twitter</td>
    </tr>
  </tbody>
</thead>