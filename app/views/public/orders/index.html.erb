<h1>注文履歴一覧</h1>


<% @orders.each do |order| %>
注文日
<%= order.created_at.strftime('%Y/%m/%d') %>
配送先
<%= order.address %>
注文商品
<%= order.item.name %>
支払金額
<%= order.total_price %>
ステータス
<%= order.is_active %> 
注文詳細
<%= link_to "表示する", order_path(order.id) %>

<% end %>